# The hackathon UI defaults to the removable in-memory adapter.
VITE_DATA_ADAPTER=mock

# To exercise the final HTTP contract locally, set the adapter to http and
# start @lorestra/api. This is a public local URL, not a credential.
VITE_LORESTRA_API_URL=http://localhost:8787
