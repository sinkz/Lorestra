{
  "name": "@lorestra/mock-vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --maxWorkers=1 --no-file-parallelism",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock.ts",
    "./fixtures": "./src/fixtures/index.ts"
  },
  "dependencies": {
    "@lorestra/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
